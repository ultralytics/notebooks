name: Run Notebooks

permissions:
  contents: read

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 8 * * *"  # Daily at 08:00 UTC

jobs:
  notebooks:
    name: Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies  # Transformer dependency for Florence-2
        run: |
          python -m pip install --upgrade pip
          pip install ultralytics pytest transformers==4.49.0

      - name: Run notebook tests
        run: |
          echo "CI event: ${{ github.event_name }}"
          cd $GITHUB_WORKSPACE  # now we're in /home/runner/work/notebooks/notebooks
          if [ "${{ github.event_name }}" = "schedule" ]; then
            echo "Running full test suite including @slow tests"
            pytest tests/test_notebooks.py
          else
            echo "Running fast tests only (excluding @slow)"
            pytest -m "not slow" tests/test_notebooks.py
          fi
